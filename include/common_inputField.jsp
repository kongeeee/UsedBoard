<%@ page contentType="text/html; charset=utf8" %>

<div class="input-field col s12">
    <i class="material-icons prefix">${param.icon}</i>
    <input id="${param.varname}" name="${param.varname}" type="text">
    <label for="${param.varname}">${param.korname}</label>
</div>